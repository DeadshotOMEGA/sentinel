# Release Labeling SOP

1. Keep issue milestone and project `Release` aligned.
2. Use `priority:p0` to signal release-critical items.
3. Before release cut, review open `type:bug` issues by priority.
4. For deferred work, keep labels accurate and retarget milestone/release.
