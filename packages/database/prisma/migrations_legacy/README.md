# Legacy Prisma Migrations Archive

These migration files were generated during the pre-baseline era and are
archived to preserve historical context.

Starting with the canonical baseline migration at
`packages/database/prisma/migrations/0_init/migration.sql`, fresh installs and
future incremental migrations use a clean migration chain.

Do not add new migrations to this archive directory.
