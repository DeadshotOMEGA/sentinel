{
  "name": "sentinel-monorepo",
  "version": "2.0.0",
  "private": true,
  "description": "RFID Attendance Tracking System for HMCS Chippawa",
  "scripts": {
    "dev": "pnpm --filter @sentinel/backend dev",
    "dev:backend": "pnpm --filter @sentinel/backend dev",
    "dev:frontend": "pnpm --filter frontend-admin dev",
    "dev:contracts": "pnpm --filter @sentinel/contracts dev",
    "dev:all": "bash scripts/ensure-services.sh && bash scripts/cleanup-ports.sh && concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"pnpm dev:backend\" \"pnpm dev:frontend\"",
    "dev:frontend-stack": "concurrently --names \"CONTRACTS,FRONTEND\" --prefix-colors \"yellow,magenta\" \"pnpm dev:contracts\" \"pnpm dev:frontend\"",
    "cleanup": "bash scripts/cleanup-ports.sh",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "prisma:generate": "pnpm --filter @sentinel/database prisma:generate",
    "prisma:migrate": "pnpm --filter @sentinel/database prisma:migrate",
    "prisma:studio": "pnpm --filter @sentinel/database prisma:studio",
    "mermaid:svg": "bash scripts/mermaid-to-svg.sh",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@playwright/test": "^1.58.0",
    "@types/node": "^24.10.9",
    "@typescript-eslint/eslint-plugin": "^8.53.1",
    "@typescript-eslint/parser": "^8.53.1",
    "@vitest/coverage-v8": "^4.0.17",
    "concurrently": "^9.2.1",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.0.0",
  "pnpm": {
    "overrides": {
      "@types/node": "^24.10.9",
      "hono": "^4.11.4"
    }
  },
  "lint-staged": {
    "apps/**/*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "packages/**/*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  }
}
